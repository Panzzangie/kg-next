import * as React from 'react'

interface Props {}

// FIXME limit route param to only accept valid values
export default function Dungeon(props: Props) {
  return <div>this is where an individual dungeon will be</div>
}
